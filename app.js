// const express = require("express");
// const app = express();
// const morgan = require("morgan");

// app.use(morgan("dev"));

// app.use(express.json());

// const allGames = (req, res) => {
//   res.status(200).json({
//     status: "Sucess",
//   });
// };

// const registration = (req, res) => {
//   res.status(404).json({ message: "hello" });
// };

// app.get("/api/games", allGames);

// app.post("/api/registration", registration);

// // app.route("api/games").get(allGames).post(registration);

// app.listen(port, () => {
//   console.log(`App is running on ${port}....`);
// });

// module.exports = app;
